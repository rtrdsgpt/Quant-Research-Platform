# Quant Research Platform — Python dependencies
# Merged from the original return-forecasting and portfolio-replication
# projects, plus new deps for the merge (statsmodels/SARIMAX, cvxpy) and
# the MLOps additions (FastAPI, MLflow, DVC).

# Data fetching
yfinance>=0.2.31
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
gnews>=0.3.7
finnhub-python>=0.3.0

# Data processing
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0

# Machine learning / forecasting
scikit-learn>=1.3.0
lightgbm>=4.1.0
xgboost>=2.0.0
statsmodels>=0.14.0

# Portfolio construction
cvxpy>=1.4.0
tensorflow>=2.15.0; python_version < "3.13"  # optional: autoencoder selection in the legacy replication mode; falls back to PCA if absent (see src/construction/selection.py)

# NLP / sentiment
transformers>=4.35.0
torch>=2.1.0
sentencepiece>=0.1.99

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Configuration
pyyaml>=6.0.1

# API service
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# MLOps
mlflow>=2.12.0
dvc>=3.50.0

# Utilities
tqdm>=4.66.0
joblib>=1.3.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.27.0  # FastAPI TestClient

# Jupyter (optional, for notebooks)
jupyter>=1.0.0
ipykernel>=6.25.0
